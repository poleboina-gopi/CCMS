{
  "name": "college-complaint-management-system",
  "version": "1.0.0",
  "description": "A modern, full-stack College Complaint Management System (CCMS) with role-based access for Students, Admins, and Staff.",
  "main": "backend/src/server.js",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "seed": "npm run seed --prefix backend",
    "clean": "npm run clean --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "complaint-management",
    "college",
    "campus",
    "react",
    "express",
    "mongodb"
  ],
  "author": "CCMS Team",
  "license": "MIT"
}
